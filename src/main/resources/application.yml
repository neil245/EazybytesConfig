spring:
  application:
    name: "configserver"
  profiles:
    #active: native
    active: git
  cloud:
    config:
      server:
        #native:
          #search_locations: "classpath:/config"
          #search_locations: "file:///Users//indranilghoshal//Documents//EazyBytesMicroservices//Config"
        git:
          uri: "https://github.com/neil245/EazybytesConfig.git"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
encrypt:
  key: "12345asdfg"

server:
  port: 8071
management:
  endpoints:
    health:
      readiness-state:
        enables: true
      liveliness-state:
        enables: true
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true